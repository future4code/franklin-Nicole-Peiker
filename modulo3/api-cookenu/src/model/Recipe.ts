export interface ICreateRecipeInputDTO {
  title: string;
  description: string;
}

export interface IRecipeDTO {
  id: string;
  title: string;
  description: string;
  createAt: string;
}
